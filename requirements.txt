fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
httpx>=0.27
pdf2image>=1.17
# v0.7.0 verified on the local bench (local_bench/) 2026-08-19: pitch equal
# to v0.6.2 (97-100% on all 7 melodic pieces), better rhythm validity and
# feature coverage, and the slur-embedding crash that forced the old v0.6.2
# pin is gone. Keep pinning exact tags: unpinned main regressed once before
# (f6feedb shipped a decoder/checkpoint mismatch).
homr @ git+https://github.com/liebharc/homr.git@v0.7.0
